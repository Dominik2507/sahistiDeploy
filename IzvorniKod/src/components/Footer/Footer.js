import React from "react";

import {FooterContainer
} from "./FooterStyle";
const Footer = (props) => {

   

    return (
        <FooterContainer>
            
        </FooterContainer>
    );
};

export default Footer;
